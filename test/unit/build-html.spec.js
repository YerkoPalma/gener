import test from 'ava'
// import { buildIndex, buildScripts } from '../../src/libs/build-html'

test('buildIndex should build index.html in current directory', t => {

})

test('buildScripts should build scripts if there are in config', t => {

})

test('buildScripts should build nothing if there is not in config', t => {

})
